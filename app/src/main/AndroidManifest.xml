<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.miwok">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".familyActivity"
            android:label="Family Members">
        </activity>
        <activity android:name=".colorsActivity" android:label="Colours"/>
        <activity android:name=".PhrasesActivity" android:label="Phrases -- USE THEM"/>
        <activity android:name=".NumbersActivity"
            android:label="NUMBERS">

        </activity>
    </application>

</manifest>